{
  "name": "news-api",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "json-server --watch db.json --port $PORT --host 0.0.0.0 --static ."
  },
  "dependencies": {
    "json-server": "^1.0.0-beta.3"
  }
}
